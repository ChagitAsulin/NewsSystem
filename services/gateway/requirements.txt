# services/gateway/requirements.txt
# ------------------------------
# Core framework
# ------------------------------
fastapi==0.115.2           # שרת אפליקציה מבוסס ASGI
uvicorn[standard]==0.30.6  # שרת הרצה ל-FastAPI עם תמיכה ב-auto reload

# ------------------------------
# Data models & validation
# ------------------------------
pydantic>=2.8,<3           # הגדרת מודלים וסכמות נתונים
confluent-kafka==2.5.0

# ------------------------------
# External integrations
# ------------------------------
requests>=2.32             # גישה ל-NewsAPI וקריאות HTTP לשירותים אחרים

# ------------------------------
# Optional dependencies (מומלץ)
# ------------------------------
python-dotenv>=1.0         # טעינת משתני סביבה מקובץ .env
loguru>=0.7                # לוגים נוחים וקריאים
aiofiles>=23.2             # ניהול קבצים אסינכרוני (אם תרצי later file upload)
httpx>=0.27                # אלטרנטיבה אסינכרונית ל-requests (לשירותים פנימיים)

# ------------------------------
# Development & testing (לא חובה בהרצה)
# ------------------------------
pytest>=8.3                # בדיקות יחידה
pytest-asyncio>=0.23       # בדיקות אסינכרוניות
black>=24.4                # עיצוב קוד אוטומטי
flake8>=7.1                # בדיקת סגנון קוד

firebase-admin==6.5.0
# Firebase Admin SDK for Firestore integration
